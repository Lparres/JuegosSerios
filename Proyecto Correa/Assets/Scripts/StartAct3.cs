using UnityEngine;

public class StartAct3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        GameManager.Instance.IntroAct(3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
